List recent Zoom meetings with cloud recordings.

Use the `list_meetings` tool from the ECHO MCP server. If the user passed a number of days as an argument (e.g. `/echo-recent 7`), use that; otherwise default to 30.

Format as a concise list with meeting title, date, and meeting ID. Highlight which ones have transcripts available.
