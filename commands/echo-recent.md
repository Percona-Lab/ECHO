---
description: List recent Zoom meetings with recordings
argument-hint: "[days=30]"
---

Use the `list_meetings` tool from the ECHO MCP server with `days=${ARGUMENTS:-30}` to show recent Zoom meetings. Format as a concise list with meeting title, date, and meeting ID. Highlight which ones have transcripts available.
