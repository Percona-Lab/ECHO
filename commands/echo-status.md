---
description: Check ECHO authentication status with Zoom
---

Use the `auth_status` tool from the ECHO MCP server to check whether ECHO is authenticated with Zoom. Report the status concisely and, if not authenticated, tell the user exactly what to do next.
